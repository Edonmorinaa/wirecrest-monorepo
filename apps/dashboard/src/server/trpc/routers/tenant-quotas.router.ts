/**
 * Tenant Quotas Router
 * 
 * tRPC router for tenant quotas and usage management.
 * 
 * Note: Most quota functionality is currently disabled/commented out in the original server actions.
 * This router provides a minimal placeholder structure for future implementation.
 */

import { router } from '../trpc';

/**
 * Tenant Quotas Router
 * 
 * Placeholder router for tenant quota management.
 * Original implementation is disabled - see apps/dashboard/src/actions/tenant-quotas.ts
 */
export const tenantQuotasRouter = router({
  // Procedures will be added here when quota management is re-enabled
  // For now, this is an empty router to maintain structure
});

