{
  "name": "@wirecrest/db",
  "version": "1.0.0",
  "main": "index.ts",
  "types": "index.d.ts",
  "packageManager": "yarn@4.0.2",
  "dependencies": {
    "@prisma/client": "6.16.2"
  },
  "scripts": {
    "generate": "prisma generate",
    "build": "echo 'No build step required for db package'",
    "dev": "echo 'No dev step required for db package'",
    "lint": "echo 'No lint step required for db package'",
    "clean": "echo 'No clean step required for db package'",
    "type-check": "echo 'No type-check step required for db package'",
    "seed:feature-flags": "tsx seed/seedFeatureFlags.ts"
  }
}
